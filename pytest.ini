[pytest]
# Add the project root to sys.path so that all imports work
# whether tests are run with: pytest, python -m pytest, or python tests/test_*.py
pythonpath = .
testpaths = tests
asyncio_mode = strict
